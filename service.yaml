---
apiVersion: v1
kind: Service
metadata:
  name: springboot-docker-kubernets
spec:
  selector:
    app: springboot-docker-kubernets
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: LoadBalancer
